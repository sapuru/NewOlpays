# Olpays.com landing page

Enviroment parametrizable, Renderer, minifier and optimizer Olpays.com landing page.

* Gulp used for task managment and NPM as a task runner. 
* No global dependencies :D

TODO: Nearly all mustache templates parameters

## Installation

npm install

## Usage

npm run build


### A "dist" folder will be generated with the static landing page.